import 'package:flutter/material.dart';

/// Barakah City color tokens.
///
/// Palette philosophy: green (growth), gold (prosperity), soft neutrals (calm).
/// No harsh reds — the game should feel peaceful and ethical.
class AppColors {
  AppColors._();

  // Brand
  static const Color green = Color(0xFF2E7D52); // growth, nature
  static const Color greenLight = Color(0xFF5FA877);
  static const Color gold = Color(0xFFD9A441); // prosperity, value

  // Surfaces / neutrals
  static const Color cream = Color(0xFFF6F1E7);
  static const Color sand = Color(0xFFEADFC8);
  static const Color sky = Color(0xFFAFD3E2);

  // Text
  static const Color textDark = Color(0xFF2B2B2B);
  static const Color textMuted = Color(0xFF6B6B6B);
}
